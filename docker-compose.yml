version: '3.8'

services:
  math-printer:
    build: .
    container_name: math-printer
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - printer-net

networks:
  printer-net:
    driver: bridge
